#include "monty.h"
/**
 * _stack - prints the top
 * @head: stack head
 * @count: line_number
 * Return: no return
*/
void _stack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus.lifi = 0;
}
